Esta pasta recebe `Poppins-ExtraBold.ttf` automaticamente pelo script `scripts/fetch_runtime.py` durante a montagem para Windows.

O arquivo da fonte não é versionado no repositório; o workflow baixa a versão oficial do Google Fonts e a inclui no pacote portátil.